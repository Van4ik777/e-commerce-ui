// login
// api.login

// register
// api.register

// Make global auth store in directoru stores auth.store.ts--> isAuth, setAuth, register(aith.service.register, setCookie(tokens)), login(auth.serive.login, , setCookie(tokens)), logout(clearCookie())

// In any component -> {isAuth, login} = useAuth()
